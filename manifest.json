{
  "name": "smtp-client integration test",
  "description": "Testing the smtp-client",
  "version": "0.0.1",
  "manifest_version": 2,
  "offline_enabled": true,
  "permissions": ["http://*/*", "https://*/*", {
    "socket": ["tcp-connect"]
  }],
  "app": {
    "background": {
      "scripts": ["test/res/background.js"]
    }
  }
}